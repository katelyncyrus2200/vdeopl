<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:keepScreenOn="true"
        android:background="#000000" />

    <!-- Center controls: EXACT Exo-style icons + amount text -->
    <LinearLayout
        android:id="@+id/centerControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="24dp"
        android:visibility="gone">

        <!-- Rewind 15 -->
        <TextView
            android:id="@+id/btnBack15"
            android:layout_width="104dp"
            android:layout_height="104dp"
            android:background="@android:color/transparent"
            android:foreground="@drawable/ic_vr_rewind"
            android:gravity="bottom|center"
            android:paddingBottom="20dp"
            android:text="15"
            android:textColor="#FFFFFFFF"
            android:textStyle="bold"
            android:textSize="18sp" />

        <!-- Play/Pause -->
        <ImageButton
            android:id="@+id/btnPlayPause"
            android:layout_width="132dp"
            android:layout_height="132dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:background="@android:color/transparent"
            android:scaleType="centerInside"
            android:src="@drawable/ic_vr_play_circle" />

        <!-- Forward 15 -->
        <TextView
            android:id="@+id/btnFwd15"
            android:layout_width="104dp"
            android:layout_height="104dp"
            android:background="@android:color/transparent"
            android:foreground="@drawable/ic_vr_fastforward"
            android:gravity="bottom|center"
            android:paddingBottom="20dp"
            android:text="15"
            android:textColor="#FFFFFFFF"
            android:textStyle="bold"
            android:textSize="18sp" />

    </LinearLayout>

</FrameLayout>
